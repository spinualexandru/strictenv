'use strict';

const {
    enableStrictEnv,
    disableStrictEnv,
    getAccessStats,
    isEnabled
} = require('./lib/dotnope');

module.exports = {
    enableStrictEnv,
    disableStrictEnv,
    getAccessStats,
    isEnabled
};
